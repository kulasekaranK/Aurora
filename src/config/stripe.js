// ⚠️  TEST STOREFRONT ONLY — secret key must never ship in a real production frontend.
//     In production: move createPaymentIntent() to a backend route and remove SECRET_KEY here.
export const STRIPE_CONFIG = {
 
  // TODO: replace with your actual Stripe secret test key (sk_test_...)
 
  COMPANY_NAME: 'Aurora Store',
}
 